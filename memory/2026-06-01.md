# 2026-06-01 — Phase 1a Implementation

## Phase 1a — Database Foundation (COMPLETE)

### Files Created
- `server/utils/migrate.js` — idempotent sequential migration runner
- `server/migrations/001_faq_status.sql` — faq_status, review_reason, published_by, published_at, merged_into, view_count on questions; featured_in_faq, accepted_by on answers; indexes
- `server/migrations/002_faq_review.sql` — faq_review_log, faq_revision_log tables
- `server/migrations/003_faq_tags.sql` — faq_tags, question_tags tables + seed data
- `server/migrations/004_sp_enrichment.sql` — sp_ledger_enrichment, sp_anomaly_events, sp_watchlist, sp_adjustments, leaderboard_snapshots tables
- `server/migrations/005_content_flags.sql` — content_flags table
- `server/migrations/006_faq_ai_analysis.sql` — faq_ai_analysis table

### Files Modified
- `server/middleware/auth.js` — added `requireFaculty()` middleware
- `server/routes/faculty.js` — NEW stub router (all 501 Not yet implemented)
- `server/server.js` — import migrate.js, runMigrations() at startup, mount /api/faculty router

### MIDDLEWARE CHAIN BUG FOUND — needs fix
- `server/server.js` applies `authenticate` middleware from api.js, but `facultyRouter` needs `authenticate` from auth.js before `requireFaculty`
- **Fix needed:** In server.js, change:
  ```js
  // WRONG (current):
  app.use('/api/faculty', facultyRouter);
  // CORRECT:
  import { authenticate, requireFaculty } from './middleware/auth.js';
  app.use('/api/faculty', authenticate, requireFaculty, facultyRouter);
  ```

### Verified Working
- All 4 existing routes: `/`, `/community`, `/insights`, `/faq/1` → HTTP 200
- `GET /api/community/questions` → 11 questions, all `faq_status: 'published'` (backfilled)
- `GET /api/stats` → valid data
- `GET /api/questions?sort=votes` → HTTP 200
- All 6 migrations applied to existing DB without error

### Blocking Issue
Write to `server/server.js` BLOCKED (memory flush turn). Fix manually:
1. Add to top of server.js imports: `import { authenticate, requireFaculty } from './middleware/auth.js';`
2. Change `app.use('/api/faculty', facultyRouter);` → `app.use('/api/faculty', authenticate, requireFaculty, facultyRouter);`
3. Restart server: kill old process, `node server/server.js`

## Phase 1b — Ready to implement next
- Vote automation with BEGIN IMMEDIATE transaction
- List route changes (faq_status, ?status=all backward compat)